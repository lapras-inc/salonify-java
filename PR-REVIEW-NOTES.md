
※企画段階で、AIにPRを出させてそれをレビューする,,,というものを考えていましたが、実際には行なわなかったので、このドキュメントは単なる残骸となります

AIによる比較。あくまで参考情報として出力。

# PR比較・レビュー観点ノート（イベント運営用・非公開推奨）

コードレビュー実演イベント用の内部資料。全10お題 × Sonnet/Haiku = 20本の実装PRについて、
差分の本質・レビュー論点・ディスカッション素材をまとめる。
**参加者に配る資料ではない**（ネタバレを含む）。

- リポジトリ: https://github.com/rockymanobi/code-review-demo
- ルール: 各Issueに機能的な受け入れ条件を明示済み（機能の充足はレビュー論点にしない）。実装者は CONTRIBUTING.md 準拠。
- 記法: 判定は ◎=そのまま使える / ○=使えるが注意点あり / △=実バグ・条件未達あり（それ自体をネタにするなら◎）

## PR一覧

| Issue | お題 | Sonnet | Haiku | Haiku判定 |
|---|---|---|---|---|
| #1 | コメント編集・削除 | PR #3 | PR #4 | ○（prompt()編集UIが下限すれすれ） |
| #5 | スレッド検索 | PR #16 | PR #24 | ◎ |
| #6 | アカウントロック | PR #14 | PR #25 | ◎ |
| #7 | 売上CSV | PR #17 | PR #27 | ◎ |
| #8 | N+1解消 | PR #23 | PR #28 | △（テスト無しリファクタ） |
| #9 | 投稿コメント | PR #19 | PR #31 | ◎（設計対比の主役） |
| #10 | 日割り請求 | PR #15 | PR #32 | △（起算日の前提が誤り＝実バグ） |
| #11 | 通知機能 | PR #21 | PR #29 | △寄り○（再公開で再通知） |
| #12 | Resendメール | PR #18 | PR #26 | △（エラー握りつぶし） |
| #13 | S3アップロード | PR #20 | PR #30 | ◎ |

---

# Issueごとの比較と論点

## #1 コメント編集・削除（PR #3 vs #4）

| | Sonnet #3 (+214) | Haiku #4 (+113) |
|---|---|---|
| 認可 | `CommentAccess` に集約+テスト7件 | コントローラ・テンプレ2箇所に条件をコピペ（計3箇所） |
| 編集UI | サーバサイド インラインフォーム（`?edit=`） | `window.prompt()` + JSでフォーム動的生成 |
| 削除表現 | `deleted` フラグのみ（本文保持） | フラグ+本文を「削除されました」で上書き（復元不能） |
| テンプレ | `commentBody` フラグメントに集約 | トップ/返信でマークアップ丸コピー |
| テスト | あり | なし |

**レビュー論点（構造）**: 認可述語の置き場所と重複 / ソフトデリートで元データを破壊するか / SSRアプリにJS依存UIを持ち込む是非 / `edited` boolean と `updatedAt` の二重持ち（#4）。

**Sonnetへの上級論点**: 認可述語は出せたが、更新/削除の*手続き*（取得→整合チェック→レート制限→mutate→redirect）は `BoardController` 直書きのまま。`JoinService`/`RevenueService` を抽出してきたこのコードベースの方針に照らすと `CommentService` に寄せる筋がある。一方「board系は元々コントローラ集約」というローカル一貫性もある。**ローカル一貫性 vs グローバル方針**のどちらを優先するかは好デモ。もう1点: コメントはソフトデリート、投稿(Post)は物理削除のままで、**削除セマンティクスがコードベース内に2種類**になった。この"方針の分裂"はPR単体では見えず、コードベース全体を見るレビューでしか拾えない。

## #5 スレッド検索（PR #16 vs #24）

| | Sonnet #16 | Haiku #24 |
|---|---|---|
| エスケープ | 自作せず Spring Data の `ContainingIgnoreCase` 自動エスケープに委譲（JARを展開して機構を確認までした） | `ThreadSearchService` で自前エスケープ + `@Query` の `LIKE ESCAPE` |
| クエリ | メソッド名導出（超長大: `findBySalonIdAndTitleContainingIgnoreCaseOrSalonIdAndBodyContainingIgnoreCaseOrderByCreatedAtDesc`） | `@Query` 明示 |

**レビュー論点**: **「フレームワークの暗黙保証に乗る」vs「明示的に自前で書く」**が真っ向対立した好例。Sonnetの委譲は依存バージョンアップで挙動が変わっても検知手段がない（テストは通り続ける）。Haikuの自前実装は追いやすいが、エスケープ自作は歴史的にバグの温床。どちらを支持するかで会場が割れる。

**Sonnetへの上級論点**: あの長大メソッド名は「命名がクエリ仕様」という Spring Data の可読性問題の見本。`@Query` か Specification に書き直すべきという指摘が妥当。さらに一歩先: LIKE '%kw%' は前方一致でないためインデックス不使用。データ量前提を聞いて pg_trgm / 全文検索へ行く話は「デモアプリにどこまでやるか」のちょうどいい入口。

## #6 アカウントロック（PR #14 vs #25）

大枠は同一設計に収束（Userにカラム追加 + ロジック切り出し + リセットで解除）。差分は細部:

| | Sonnet #14 | Haiku #25 |
|---|---|---|
| 置き場所 | `service/LoginAttemptService` | `support/AccountLockingService` |
| 期限切れ後の再カウント | 期限切れなら0にリセットしてから数え直す（明示対応） | 要diff確認（対応が曖昧） |

**レビュー論点**: `service/` と `support/` の役割分担基準がこのコードベースで言語化されていないこと自体が発見。両者が違う場所を選んだ＝**規約が暗黙だとAIごとにブレる**という実例。

**Sonnetへの上級論点**: 認証スロットリング状態を **ドメインの `User` エンティティに載せるべきか**。`failedLoginAttempts` は認証インフラの関心事で、User の属性ではないという立場なら別テーブル（`login_attempts`）が筋。さらに `ddl-auto: update` 頼みの暗黙マイグレーション（このリポジトリ全体の問題）に触れると、Flyway/Liquibase 導入をいつ決断するかという「デモアプリの成長痛」の話になる。

## #7 売上CSV（PR #17 vs #27）

| | Sonnet #17 | Haiku #27 |
|---|---|---|
| CSV部品 | `support/Csv`（汎用） + `RevenueExportService` | `CsvExportService`（サービス層に配置） |
| 集計 | 既存 `RevenueService`/`Payment` 再利用 | 同（ただし `RevenueService` 本体に `invoicesForCsv()` を追加＝既存サービス肥大） |
| インジェクション対策 | クォート前置 | クォート前置（`=` `+` `-` `@` の4種と明示） |

**レビュー論点**: 責務分割の粒度（新サービスを立てる vs 既存に足す）。両者とも品質は高い。

**Sonnetへの上級論点**: (1) 全件をメモリ上で組み立てる実装 — 請求10万件でどうなるか、ストリーミング書き出しにすべきかは「今やるか、要るときやるか」のYAGNI議論に最適。(2) CSVインジェクション対策のクォート前置は**データの真値を変える**（`=SUM` という名前のプラン名が `'=SUM` になる）。安全性と忠実性のトレードオフで、"正解が状況依存" な指摘の練習になる。(3) 「Excelで開ける=BOM付きUTF-8」というドメイン知識がどのクラスに封じられているべきか。

## #8 N+1解消（PR #23 vs #28）— リファクタPRの品質差

| | Sonnet #23 (+498) | Haiku #28 (+99) |
|---|---|---|
| 構造 | `BoardQueryService` を抽出しテスト可能に | `BoardController` 内で置き換え（構造は据え置き） |
| クエリ記法 | Spring Data 流儀に準拠 | **native SQL** (`nativeQuery = true`) が混入 |
| 計測 | 実Postgres+SQLログで実測、行数を変えて式まで検証（22→4、49→7） | 数値はあるが式ベースの見積もりに見える |
| テスト | 新規9件 | **テストファイル変更ゼロ**（CONTRIBUTING「純リファクタでも挙動固定テストを」に違反） |

**レビュー論点**: 「挙動を変えないPR」の信頼をどう作るか。Sonnetは計測+テストで証明を積み、Haikuは主張のみ。**同じ green でも証明力が違う**。また Haiku の native SQL は JPQL/導出メソッドで統一されたコードベースに方言を持ち込んだ（エンティティ名変更に追随しない）。

**ディスカッション素材**: Haiku #28 は「テストは全部通ってます、挙動変えてません」と主張する差分最小のリファクタPR。**あなたはこれを通すか?** — AIのPR受け入れ基準の議論にちょうどいい。

## #9 投稿コメント（PR #19 vs #31）— データモデルの真っ二つ

| | Sonnet #19 | Haiku #31 |
|---|---|---|
| データモデル | **新テーブル `PostComment`**（`PostReaction` が別テーブルという既存前例を根拠に） | **既存 `Comment` に nullable `postId` を追加**して `threadId` と並立 |
| 共通化 | フラット化ロジックを `support/Comments` に抽出、テンプレを共通フラグメント化 | 掲示板のマークアップを post-detail に再現（流用） |
| おまけ | **既存バグ発見**: 投稿詳細に下書きチェックが無く直URLで非オーナーが閲覧可能 → 404ガード追加 | なし |

**レビュー論点（本命）**: nullable FK並立は「thread と post のどちらか片方だけがセットされる」という不変条件を型でもDBでも守れない（両方null/両方セットが表現可能）。一方テーブル分離は将来「コメント編集・削除(#1)を両方に付ける」際に実装が2倍になる。**3つ目のコメント対象が現れたらどうするか**まで延ばすと polymorphic association の一般論に接続できる。

**Sonnetへの上級論点**: (1) 共通フラグメントが2つの view model の「たまたま同名のフィールド」に依存する構造（Thymeleaf のダックタイピング）。コンパイル時に守られない契約で、フィールド名を変えると実行時に割れる。共有インターフェイスを切るべきか。(2) **スコープ外のバグ修正（下書き閲覧穴）を同じPRに含めた**。修正自体は正しいが、人間のチームなら「別PRに切って」と言う場面。AIは見つけたら直す傾向がある — 「AIのPRのスコープ管理」というメタ話題への最高の入口。

## #10 日割り請求（PR #15 vs #32）— 前提確認の有無

| | Sonnet #15 | Haiku #32 |
|---|---|---|
| 課金起算日 | 「既存は変更日を追跡していない」と確認し **`planChangedAt` カラムを新設**、プラン変更時に更新 | 「`joinedAt` はプラン変更時に更新される」**と誤認**して既存カラムを流用 |
| 計算 | 副作用のない static メソッド + 境界値テスト12件 | ほぼ同型のメソッド + テスト14件 |

**確定済みの実バグ（#32）**: main のプラン変更パスは `joinedAt` を更新しない（検証済み）。Haiku 版は Issue の「課金起算日 = 直近の変更日」を満たさず、**2回目以降のアップグレードで残日数が入会日基準のまま計算される**（30日超過後は常に0円請求）。しかも**テスト14件は全部 green** — テストが実装の誤った前提ごと写しているため。

**ディスカッション素材（最重要級）**: 「テストが緑＝正しい、ではない」の完璧な実例。AIの自己申告（受け入れ条件✅全充足）とテスト green が揃っていても、**前提事実の検証はレビュアーの仕事として残る**。イベントの山場に使える。

**Sonnetへの上級論点**: (1) 30日固定の月定義 — 実カレンダーとズレる。仕様が30日と言ったからそのまま実装したが、"仕様の疑い方" の議論に。(2) 日割り Invoice に種別が無く、金額だけでは初回請求と区別できない（会計・CS対応の説明可能性）。`Invoice.kind` を切るべきか。(3) 分岐が増え続ける `JoinService#join`（モック/Stripe/トライアル/日割り…）— `PlanChangePolicy` 的な抽出の頃合い。

## #11 通知機能（PR #21 vs #29）— アーキテクチャの真っ二つ

| | Sonnet #21 | Haiku #29 |
|---|---|---|
| 方式 | **通知テーブル無し・読み取り時計算**（`Post.publishedAt` + `User.notificationsReadAt` の2タイムスタンプ） | **通知テーブル + 書き込み時ファンアウト**（`@Async` でバックグラウンド生成） |
| 公開操作 | O(1)（会員数無関係） | API即応答だが裏で会員数ぶんINSERT |
| 二重通知防止 | `publishedAt` は初回のみセット、以後不変 | **再公開時に旧通知を削除して再生成** |

**確定済みの問題（#29）**: 公開→下書き→再公開で `deleteByPostId` → 再INSERT が走るため、**既読済みの会員にも未読として再通知される**（通知の「件数」は増えないが実質的な再通知）。受け入れ条件の意図に反する。加えて `@Async` デフォルトエグゼキュータ任せ・途中クラッシュ時のリカバリ無し。

**レビュー論点（本命）**: 通知を「データ」として持つか「導出」にするかの根本対立。Sonnet 案はエレガントだが、(1) 個別既読・通知種別追加・プッシュ通知などの将来要件に耐えない（通知という実体が無い）、(2) `notificationsReadAt` が User に載る（全サロン一括既読しかできない）。Haiku 案は王道だが上記の実装瑕疵。**「正しいアーキテクチャを瑕疵ありで実装した Haiku」vs「割り切ったアーキテクチャを正しく実装した Sonnet」のどちらを取るか** — シニア討論の主菜。

## #12 Resendメール（PR #18 vs #26）

| | Sonnet #18 | Haiku #26 |
|---|---|---|
| HTTP | 標準 `java.net.http.HttpClient`（依存追加ゼロ） | `RestTemplate`（+ WebConfig に Bean 追加） |
| 失敗時 | `EmailSendException` → コントローラで捕捉 → エラー付きリダイレクト（再試行導線まで場面別に設計） | **catch して log のみ**。ユーザーには成功と同じ画面 |
| テスト | HttpClient モック8件 | RestTemplate モックあり |

**確定済みの条件未達（#26）**: 送信失敗がユーザーに一切伝わらない（「コードを送信しました」と出てメールは来ない）。受け入れ条件「再試行できる形でエラーが伝わる」に明確に違反。**自己申告の充足表では✅になっていた**点も込みでネタになる。

**Sonnetへの上級論点（インターフェイス切り直しの話）**: 現状の `EmailSender` は1クラス内で `if (enabled())` 分岐する具象クラス。これを
`EmailSender`（インターフェイス）+ `ResendEmailSender` / `DevConsoleEmailSender` の2実装に分け、`@ConditionalOnProperty` で選択する設計と比較すると:
- 分岐が構成時に1回だけ評価され、実行時コードから if が消える
- モック実装の存在が型として明示される（現状は「ログに出す」がプロダクションコードの中に同居）
- 一方このアプリの規模で2実装+条件Beanは過剰では? という YAGNI 側の反論も成立
さらに `AuthPageController` が「キー未設定なら画面にコードを出す」判定を別途持っており、**「モックモードである」という知識が2箇所に漏れている**。これはインターフェイスを切ればプロバイダ側に封じられる — 抽象化の実利を示す具体例として使える。発展: 送信失敗時にコードは保存済みなので「メールは失敗したが登録は進む」— Outbox パターン / 結果整合の話への入口。

## #13 S3アップロード（PR #20 vs #30）

| | Sonnet #20 | Haiku #30 |
|---|---|---|
| 構造 | `UploadStorageService` + `S3Client` を `@Configuration` Bean 化（モック注入可能） | `ImageUploadService` に環境変数4つを直接注入 |
| 認証情報 | SDK のデフォルトプロバイダチェーンに委譲 | `AWS_ACCESS_KEY_ID` 等を**自前で読んで判定**（IAMロール等で動かない） |
| 公開方法 | **`PUBLIC_READ` canned ACL** | 仮想ホスト形式URL（ACL明示なし） |
| 発見事項 | Issue の「5MB」に対し実上限4MBと突き止め、挙動維持を優先して4MBのまま(PRに明記) | — |

**レビュー論点**: (1) Sonnet の `PUBLIC_READ` ACL は現行のS3デフォルト（ACL無効・Block Public Access）では動かない時代遅れの方式。バケットポリシー/CloudFront/署名付きURLへの誘導。(2) Haiku の環境変数直読みは、EC2/ECSのIAMロールやSSO等**チェーンの他の供給源を全部殺す** — 「SDKの流儀を知らないコード」の典型。(3) 両者とも**絶対URLをDBに保存**する設計 — バケット移行やCDN導入でURLが全部死ぬ。キーだけ保存して表示時に解決すべきという指摘は保守性の教科書ネタ。(4) Sonnet の「Issueの5MB表記 vs 実装4MB」への対応は、**仕様書とコードが食い違うとき実装者はどうすべきか**という良い小話。

---

# Haiku はどこまで使えるか（許容範囲の判定）

**結論: 「流石にレベル低すぎ」の域のPRは1本も無い。** 全PRがコンパイル・テスト green・受け入れ条件をほぼ充足し、体裁も揃っている。差が出るのは (a) 前提事実の検証、(b) エラーパスの設計、(c) リファクタの証明力、(d) フレームワーク流儀の知識。

| 区分 | PR | 使い方 |
|---|---|---|
| **そのまま対比ネタに使える** | #24 検索 / #25 ロック / #27 CSV / #30 S3 / #31 投稿コメント | Sonnetと並べて設計判断の違いを議論。品質面の不安なし |
| **意図的な"仕込み"として最高** | #32 日割り（前提誤認の実バグ+green テスト）/ #26 メール（エラー握りつぶし）/ #28 N+1（テスト無し・native SQL）/ #29 通知（再通知バグ） | 「AIの自己申告をどう検証するか」の演習素材。**バグ探しではなく"申告の抜き打ち検証"としてレビューさせる**と締まる |
| **下限すれすれ** | #4 コメント編集（`window.prompt()` UI、認可3箇所コピペ） | 唯一「レベル低い」と言われうる。ただし受け入れ条件*前*の実装なので、出すなら経緯込みで |

**モデル選定の目安**として一般化すると: S〜M規模で仕様が受け入れ条件に閉じているお題（検索・CSV・ロック・S3）は Haiku で十分イベントに耐える。**既存コードの前提を調べてから作る必要があるお題（#10）や、エラーパス・運用まで設計が及ぶお題（#11, #12）は Haiku が「もっともらしく間違える」**——これはリスクでもあり、演出意図があるなら最良の教材でもある。

---

# 横断ディスカッション素材:「AIからのPR、どこまでこだわるべき？」

1. **個々は合格、総体は腐る（一貫性ドリフト）**: この20本を全部マージすると——HTTPクライアントが3流儀（HttpClient/RestTemplate/SDK）、削除セマンティクス2種、`service/` vs `support/` の揺れ、native SQL の混入——が一晩で発生する。PR単体レビューでは検知できない劣化。**AI時代のレビューは「このPRは正しいか」から「このPRはコードベースの勾配をどちらに向けるか」へ**、が本イベントの結論候補。
2. **テスト green ≠ 正しい**: #32 はテスト14件greenで実バグ。テストは実装の前提を写すだけ。人間の仕事は「前提の事実確認」に移る。
3. **自己申告の検証**: #26/#28 は充足表で✅を主張して未達だった。レビューは「主張された✅の抜き打ち監査」という運用が現実的。
4. **スコープ管理**: #19 は既存バグを見つけて同梱修正した。人間なら別PR。AIのPRでは頻発する。チーム規約をどう決めるか。
5. **「気になるけど通すかも?」の実例集**: #16 の長大メソッド名（動くし読める、でも…）/ #20 の PUBLIC_READ（デモ環境なら動く）/ #21 の `notificationsReadAt` が User 直載り（今の要件では困らない）/ #27 の全件オンメモリCSV（今のデータ量なら困らない）。**"今は困らない" をいくつまで許すと保守性が死ぬか**、参加者に線を引かせると盛り上がる。
6. **暗黙規約はAIに効かない**: #6 の service/support 分裂が示すとおり、CONTRIBUTING に書いていない規約は毎回ブレる。「AIと働く=規約を明文化するコスト」の話。

# 進行のおすすめ

1. **開幕**: #5 ペア（小さく、思想の違いが一目でわかる: 委譲 vs 自前）
2. **主菜1**: #9 ペア（データモデル討論: 新テーブル vs nullable FK）
3. **主菜2**: #11 ペア（アーキテクチャ討論: 導出 vs 実体+ファンアウト）
4. **山場**: #32 の「greenなのにバグ」を実際にレビューで発見してもらう
5. **締め**: 横断素材1（一貫性ドリフト）→「どこまでこだわるべき?」討論
