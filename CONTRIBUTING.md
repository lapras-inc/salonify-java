※
※企画段階で、AIにPRを出させてそれをレビューする,,,というものを考えていましたが、実際には行なわなかったので、このドキュメントは単なる残骸となります

# CONTRIBUTING — 実装者向け共通ルール

このリポジトリはコードレビュー実演イベント用のデモサービス（Salonify / Java・Spring Boot）です。
Issue に対して Pull Request を作り、その PR をレビューする、という流れで運用します。

**実装に着手する前に、必ずこのファイルを最後まで読み、以下のルールすべてに従ってください。**
Issue 本文の「受け入れ条件」が満たすべき挙動、このファイルが守るべき進め方です。両方を満たして初めて完成とみなします。

---

## 1. 変更してよい範囲

- 変更は `app/` 配下、およびルートの `docker-compose.yml` / `README.md` に閉じること。
- **`refs/` と `docs/` は変更しない。** これらは参照実装と仕様の凍結資料です。
- `docker compose up` 一発で起動できる状態を壊さない（ホストに Docker 以外を要求しない）。

## 2. 既存の設計・流儀に従う

新しい仕組みを持ち込む前に、まず既存コードがどうやっているかを読んで合わせること。

- **認証・認可**: Spring Security 本体は使わない。JWT Cookie + Interceptor + 自前ガード（`support/Auth` の `requireSalonMember` / `requireSalonOwner` / `requireAdmin` 等）を踏襲する。ログインユーザー取得・権限判定は既存の作法を再利用する。
- **サニタイズ**: 本文の HTML 化は「表示時に `Sanitizer#renderMarkdown` を通す」既存方式に合わせる（保存時に別処理を足さない）。ユーザー入力を新規に扱う箇所では、既存の投稿・コメントと**同じ経路**（サニタイズ・文字数制限・レート制限）を通すこと。
- **"変な仕様" はそのまま**: 権限エラーを 404 で隠す、フォーム名 `bodyHtml` に Markdown が入る、停止は passwordHash 上書き等は**バグではなく仕様**。勝手に「改善」しない。既存の 404-hides-existence の流儀に合わせる。
- **ロジックの置き場所**: ドメインロジックは `service/` のサービスクラスや `support/` の部品に寄せる方針で拡張されてきた（changeability のため）。コントローラに手続きを積み増すより、既存のサービス/部品の粒度に合わせて配置できないか検討すること。
- **認可判定の重複を避ける**: 「誰がこれをできるか」の条件を、コントローラ・テンプレート・複数箇所にコピペしない。1 箇所（テスト可能な部品）に集約する。
- **画面**: Thymeleaf のサーバーサイドレンダリング。JS への不必要な依存を新規に持ち込まない。共通化できるマークアップはフラグメントに切り出す。

## 3. テスト（必須）

- **追加・変更したドメインロジックには JUnit 5 の単体テストを書くこと。** 特に、権限判定・金額計算・境界値・サニタイズ・可視性など「間違えると挙動が変わる」ロジックは必ずテストで固定する。
- 純リファクタ（挙動を変えない変更）の場合も、挙動を保証するテストがあること（無ければ先に足してから触る）。
- **既存テストを壊さないこと。** 全テストが green であることを PR 前に確認する。
- テストは実装と同じ PR に含める。

## 4. ビルド・テストの実行

ホストに Java 21 があれば:

```bash
cd app && ./mvnw -q test
```

無ければ Docker 経由:

```bash
docker run --rm -v "$PWD/app":/build -w /build maven:3.9-eclipse-temurin-21 mvn -q test
```

コンパイル/テストが通らない状態で PR を出さないこと。

## 5. ブランチ・コミット・PR

- **`main` に直接コミットしない。** クリーンな `main` から作業ブランチを切る（例: `feature/<短い説明>`）。
- コミットメッセージは、何を・なぜ変えたかが分かる粒度で。末尾に実装者を示す `Co-Authored-By:` 行を付ける。
- **PR タイトルは `[#<Issue番号>] <説明> [<モデル名>]` の形式にすること**（例: `[#4] 掲示板コメントの編集・削除を追加 [Sonnet]`）。
  - **先頭**に対象 Issue の番号を `[#4]` の形式で付ける（ソートしやすくするため）。
  - **末尾**に実装したモデル名を `[Haiku]` / `[Sonnet]` / `[Opus]` などの形式で付ける（どのモデルが実装したかを一目で分かるようにするため）。
- PR 本文は日本語で、対応 Issue（`Closes #N` / 複数実装が並ぶ場合は `Refs #N`）と、**受け入れ条件をどう満たしたか**、**どう実装したか（設計判断）** を書く。
- レビューされる前提なので、レビュアーが設計判断を追えるように書く。

## 6. 完了の定義

以下がすべて満たされて初めて「完成」:

- [ ] Issue の受け入れ条件をすべて満たしている
- [ ] 追加・変更ロジックに単体テストがあり、全テストが green
- [ ] `app/` 内に閉じ、`refs/` / `docs/` を変更していない
- [ ] `docker compose up` 一発起動を壊していない
- [ ] ブランチを切って PR を作成済み
